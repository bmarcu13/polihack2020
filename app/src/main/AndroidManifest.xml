<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.polihack2020bylos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DoctorApp.PatientList.PatientDisplayActivity" />
        <activity android:name=".UserSignupActivity" />
        <activity android:name=".UserLoginActivity" />
        <activity android:name=".UserApp.UserMenuActivity" />
        <activity android:name=".DoctorApp.PatientList.AddPatientActivity" />
        <activity android:name=".DoctorApp.PatientList.PatientsListActivity" />
        <activity android:name=".DoctorApp.DoctorMenuActivity" />
        <activity android:name=".DoctorLoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".StartupPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>